# Changelog

## 0.1.0.0

Initial release, extracted from the repository it was written in. Ports the
PureScript `templating` package's `Templating.Ast` / `Templating.Parser` /
`Templating.Eval` to megaparsec + aeson.
